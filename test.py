points = ["A", "B", "C", "D", "E"]  # Replace with your list of points

for i in range(len(points) - 1):
    current_point = points[i]
    next_point = points[i + 1]
    print(f"From {current_point} to {next_point}")
